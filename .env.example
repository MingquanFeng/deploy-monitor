WEBHOOK_TOKEN=change-me-to-a-long-random-string

# SQLite 数据文件所在目录，不设置则默认用项目根目录下的 data/
# Docker 部署时设为 /app/data（docker-compose 已把命名卷挂到该路径）
# DATA_DIR=/app/data

# Server酱 微信推送通知。部署状态从「进行中」变为「失败」时推送。
# 注册地址 https://sct.ftqq.com/ -> 登入后获取 SendKey
# SERVERCHAN_KEY=
